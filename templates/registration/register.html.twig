<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Inscription – Retro Commerce</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
<header class="flex items-center justify-between p-4 shadow-md bg-[#e5f7f8]">
  <div class="flex items-center space-x-2">
    <img src="{{ asset('uploads/produits/im.png') }}" alt="logo" class="w-8 h-8">
    <span class="font-bold text-lg text-[#9b0d1f]">Retro Commerce</span>
  </div>
  <nav class="space-x-6">
    <a href="{{ path('app_home') }}" class="text-sm font-medium text-[#1e1e1e]">Accueil</a>
    <a href="{{ path('app_produit_index') }}" class="text-sm font-medium text-[#1e1e1e]">Vinyles</a>
    <a href="{{ path('panier_index') }}" class="text-sm font-medium text-[#1e1e1e]">Panier</a>
  </nav>
</header>

<section class="min-h-screen flex items-center justify-center px-4 bg-gradient-to-br from-[#1e1e1e] via-[#9b0d1f] to-[#e5f7f8]">
  <div class="max-w-md w-full bg-white p-8 rounded-lg shadow-lg">
    <h2 class="text-3xl font-bold text-center text-[#9b0d1f] mb-8">Créer un compte</h2>

    {{ form_start(registrationForm, { attr: { class: 'space-y-6' } }) }}
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">Adresse e-mail</label>
        {{ form_widget(registrationForm.email, { attr: { class: 'w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-[#9b0d1f]' } }) }}
        {{ form_errors(registrationForm.email) }}
      </div>

      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-1">Mot de passe</label>
        {{ form_widget(registrationForm.plainPassword, { attr: { class: 'w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-[#9b0d1f]' } }) }}
        {{ form_errors(registrationForm.plainPassword) }}
      </div>
    <div class="flex items-start gap-2">
        {{ form_widget(registrationForm.agreeTerms, {
            attr: {
            class: 'mt-1 rounded border-gray-300 text-[#9b0d1f] focus:ring-[#9b0d1f]'
            }
        }) }}
        <label for="{{ registrationForm.agreeTerms.vars.id }}" class="text-sm text-gray-700">
            J’accepte les <a href="#" class="text-[#9b0d1f] font-medium hover:underline">conditions générales</a>.
        </label>
    </div>
        {{ form_errors(registrationForm.agreeTerms) }}

      <button type="submit"
        class="w-full bg-[#9b0d1f] text-white py-2 rounded hover:bg-[#b31226] transition duration-200 font-semibold">
        S'inscrire
      </button>
    {{ form_end(registrationForm) }}

    <p class="text-center text-sm text-gray-600 mt-6">
      Déjà un compte ?
      <a href="{{ path('app_login') }}" class="text-[#9b0d1f] font-semibold hover:underline">Se connecter</a>
    </p>
  </div>
</section>

</body>
